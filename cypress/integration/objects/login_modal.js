//const modal = '.minimal-body'

module.exports = {
  //modal : modal,
  // emailField : `${modal} #email-wrapper`,
  // passwordField : `${modal} #password`,
  // loginButton : `${modal} #loginButton`,
  // errorMessageEmail : `${modal} #responseMessage`,
  // errorMessagePassword : `${modal} #password-wrapper div`,
  emailField : '#email-wrapper',
  passwordField : '#password',
  loginButton : '#loginButton',
  errorMessageEmail : '#responseMessage',
  errorMessagePassword : '#password-wrapper div'
}
